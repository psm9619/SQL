
create table medicine (
	STND_Y INT,
	IDV_ID BIGINT,
	KEY_SEQ BIGINT,
	SEQ_NO INT,
	SEX INT,
	AGE_GROUP INT,
	SIDO INT,
	RECU_FR_DT BIGINT,
	GNL_NM_CD string,
	DD_MQTY_FREQ FLOAT,
	DD_EXEC_FREQ FLOAT,
	MDCN_EXEC_FREQ FLOAT,
	UN_COST INT,
	AMT INT)
partitioned by (year INT)
ROW FORMAT DELIMITED 
    FIELDS TERMINATED BY ','
    LINES TERMINATED BY '\n'
    STORED AS TEXTFILE; 

-------------
LOAD DATA INPATH ¡®(    ).csv¡¯
OVERWRITE INTO TABLE medicine
PARTITON(year= '2013');
